---
title: 1288. 删除被覆盖区间
code_block_shrink: false
date: 2026-07-06 15:18:58
updated: 2026-07-06 15:18:58
excerpt: leetcode.1288
tags: 算法 leetcode
---
# 题目
给你一个区间列表，请你删除列表中被其他区间所覆盖的区间。

只有当 c <= a 且 b <= d 时，我们才认为区间 [a,b) 被区间 [c,d) 覆盖。

在完成所有删除操作后，请你返回列表中剩余区间的数目。

 

示例：

输入：intervals = [[1,4],[3,6],[2,8]]
输出：2
解释：区间 [3,6] 被区间 [2,8] 覆盖，所以它被删除了。
 

提示：​​​​​​

1 <= intervals.length <= 1000
0 <= intervals[i][0] < intervals[i][1] <= 10^5
对于所有的 i != j：intervals[i] != intervals[j]

# 思路

1. 先对左端点进行排序，如果左端点相同，则对右端点进行排序
2. 这样可以确保从左到右，只需要下一个的右端点是否大于上一个的右端点，如果大于说明没有被上一个包含

# 代码

```ts
function removeCoveredIntervals(intervals: number[][]): number {
    // 先对左端点进行排序，如果左端点相同，则对右端点进行排序
    intervals.sort((a, b) => {
        if (a[0] === b[0]) {
            return b[1] - a[1];
        }
        return a[0] - b[0];
    });
    let count = 0;
    let prevEnd = 0;
    for (const [start, end] of intervals) {
        // 如果当前区间的右端点大于前一个区间的右端点，则说明当前区间没有被覆盖
        if (end >  prevEnd) {
            count++;
            prevEnd = end;
        }
    }
    return count;
};

console.log(removeCoveredIntervals([[1,4],[3,6],[2,8]])); // Output: 2

// [1,4], [2,8], [3,6]
```